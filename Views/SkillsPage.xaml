<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CharactersOfCthulhu.ViewModels"
             x:DataType="viewmodels:SkillsPageViewModel"
             x:Class="CharactersOfCthulhu.Views.SkillsPage"
             Title="Allocate Skills">

    <Grid RowDefinitions="*,Auto">

        <Label 
            Grid.Row="0"
            Text="Skill Page (Under Construction)"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Grid Grid.Row="1" 
              ColumnDefinitions="*,*"
              Padding="10">

            <Button Grid.Column="0"
                    Text="Back"
                    Command="{Binding GoBackCommand}"
                    HorizontalOptions="Start" />

            <Button Grid.Column="1"
                    Text="Next"
                    Command="{Binding GoToNextPageCommand}"
                    HorizontalOptions="End" />
        </Grid>
    </Grid>

</ContentPage>